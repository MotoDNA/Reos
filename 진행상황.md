# Re:O-S 진행상황

무엇을 했는지가 아니라 **왜 그렇게 했는지**를 날짜순으로 적습니다.
지금 상태는 [REOS.md](REOS.md) 에 있습니다.

---

## 2026-09-06 — 만들기 시작해서 Phase 1 까지

제작기획사(패키지·문구·스케치북·노트·다이어리·굿즈)용 외주관리 앱을
DNA Labs 네 번째 서비스로 만들었습니다.

### 왜 Next.js 를 버리고 파일 하나로 갔나

처음엔 지시서대로 Next.js + Prisma + PostgreSQL 로 뼈대를 잡기 시작했습니다.
그러다 "Re:Bind·Re:Call·Re:Store 와 같은 홈페이지에서 쓰게" 해 달라는 말을 듣고
전부 지웠습니다.

형제 셋은 **파일 하나짜리 앱 + 같은 Supabase + 같은 계정 목록**입니다.
여기에 딴 뼈대를 하나 더 들이면 계정을 두 벌 관리하게 되고,
로그인 화면이 둘이 되고, 배포하는 방법이 둘이 됩니다.
쓰는 사람에게는 "같은 회사 서비스인데 하나만 다르게 생긴 것" 이 됩니다.

그래서 형제들과 같은 얼개로 다시 지었습니다.

### 왜 `/os` 인가 · 왜 옛 주소가 없나

형제 셋은 옛 주소(`rebind.dnalabs.kr` …)가 살아 있고 `dnalabs.kr/*` 가
그것을 비추는 구조입니다. 밖으로 나간 공유 링크 때문에 못 끄는 것입니다.

Re:O-S 는 새로 만든 앱이라 나간 링크가 없습니다.
이름표(도메인)를 하나 더 사고 DNS 를 건드릴 이유가 없어서
**`dnalabs.kr/os` 를 본집으로** 두고, rewrite 가 GitHub Pages 를 바로 비춥니다.

대신 공유 링크가 github.io 주소로 나가면 문지기가 CORS 로 막습니다.
그 주소는 `ALLOWED_ORIGIN` 에 없기 때문입니다.
앱이 링크를 만들 때 `https://dnalabs.kr/os` 로 못 박아 두었습니다.

### 표 이름에 `os_` 를 붙인 이유

Re:Bind 가 이미 `projects` 를 쓰고 있습니다. 제작기획사의 "프로젝트" 와
제본소의 "프로젝트" 는 담는 것이 전혀 다른데 이름이 같으면 둘 중 하나가
못 들어옵니다. 형제가 늘수록 이 충돌은 더 자주 납니다.

### 금액을 처음부터 딴 표로 뺀 이유

Re:Bind 는 나중에 `project_money` 로 나눴고, Re:Store 는 아직 화면에서만
가려서 개발자 도구를 열면 보입니다(RESTORE.md 11장 구멍 1).

제작기획사에서 원가와 이익은 직원에게 안 보이는 것이 보통이고,
"나중에 나누자" 는 실제로는 안 나눠집니다. 그래서 처음부터
`os_money` · `os_cost_items` · `os_quote_money` · `os_rfq_money` · `os_po_money`
다섯으로 갈라 두었습니다.

읽기는 `os_money_ok()`, 쓰기는 `is_admin()`.
`os_money_ok()` 는 **Re:Bind 의 `company_settings.staff_money` 칸을 그대로**
봅니다 — 한 회사가 두 서비스를 쓰는데 "Re:Bind 금액은 열고 Re:O-S 금액은
닫는" 경우를 아직 못 봤고, 칸을 따로 만들면 설정 화면이 둘로 늘어납니다.

### 역할을 일곱으로 안 나눈 이유

지시서에는 ADMIN·PM·SALES·PRODUCTION·QC·ACCOUNTING·VIEWER 일곱이었습니다.
`profiles.role` 은 **네 서비스가 함께 쓰는 칸**이라 여기서 값을 늘리면
형제 셋이 모르는 값을 보게 됩니다. 지금 쓰는 회사에 그만한 사람도 없습니다.
관리자/직원 둘로 두고, 필요해지면 그때 넷이 함께 늘리기로 했습니다.

### 원가 금액이 셋인 이유 (예상·확정·실제)

한 칸에 덮어쓰면 "견적 대비 얼마나 틀어졌나" 를 영영 못 셉니다.
그게 제작기획사가 가장 알고 싶어 하는 숫자입니다.
총원가를 셀 때는 항목마다 **실제 → 확정 → 예상** 차례로 있는 값을 씁니다.

### 사양을 덮어쓰지 않고 판으로 쌓은 이유

제작 중에 사양이 바뀌는 것은 예외가 아니라 기본입니다.
"몇 판으로 발주했는가" 를 못 대면 불량 책임을 가릴 수 없습니다.
새 판은 앞 판을 베껴 와 고칠 것만 고칩니다 — 빈 칸에서 다시 시작하면
안 바뀐 것까지 다시 적어야 하고, 그러다 빠뜨립니다.
FINAL 은 한 판만 둡니다. 둘이 되면 다시 흐려집니다.

### 일을 하면 상태가 따라오게 한 이유

실무자가 상태를 손으로 바꾸게 두면 아무도 안 바꿉니다.
발주서를 만들면 → 발주 단계 + 원가에 외주 제작비 한 줄,
견적을 수주로 확정하면 → 계약 단계 + 판매금액.
같은 숫자를 두 번 적게 하면 한 번은 반드시 빠집니다.

⚠ **뒤로는 안 갑니다.** 샘플을 하나 더 등록했다고 생산 중인 프로젝트가
샘플로 되돌아가면 화면 보는 사람이 무엇이 맞는지 모릅니다.

### 외주업체 포털에 핀을 둔 이유

고객사는 보기만 하니 링크로 충분합니다.
외주업체는 납기와 진행을 **씁니다.** 링크는 카톡으로 돌아다니고
담당자는 바뀝니다. Re:Store 의 점주와 같은 판단으로 핀을 한 겹 더 뒀습니다.

틀린 횟수는 **표(`os_pin_tries`)** 에 셉니다.
Re:Store 가 함수 메모리(Map)에 셌다가, Edge Function 이 쉬면 내려갔다
새로 뜨면서 센 것이 0 으로 돌아가 "10번 틀리면 잠김" 이 잘 안 걸렸습니다.
같은 실수를 하지 않으려고 처음부터 표로 만들었습니다.

### 문지기가 가리는 것

- 고객사에게 → 원가·외주업체·내부 메모가 **안 나갑니다.**
  어디에 맡겨 얼마에 만드는지는 제작기획사의 밥줄입니다.
- 외주업체에게 → **고객사가 안 나갑니다.** 최종 발주처를 알면 다음엔
  직접 붙습니다. 다른 업체의 견적도 안 나갑니다.
- 업체가 보낸 값 중 **날짜와 메모만** 받습니다. 수량·단가·우리가 정한 납기는
  받지 않습니다 — 발주 조건을 업체가 바꾸면 안 됩니다.

### 형제 셋을 함께 고쳤습니다

로그인 토글이 셋에서 **넷**이 됐습니다. 네 앱이 같은 차례·같은 문구를
써야 해서 `bindery.html` · `network-dna.html` · `store.html` 을 함께 고쳤습니다.

**뒤에 붙였습니다.** 앞에 끼우면 기기에 저장된 선택(`dnalabs-app` 과
토글 자리 `i0`·`i1`·`i2`)이 딴 것을 가리킵니다.

390px 폰에서 한 칸이 78px 밖에 안 남아, 좁은 화면에서는 설명줄을 감추고
이름과 그림만 둡니다. PC 에서는 다시 보입니다.

**덤으로 고친 것 하나** — Re:Call 의 `ONE_ROOF` 에 `restore:null` 이
들어 있었습니다. `dnalabs.kr/call` 에서 Re:Store 를 고르고 로그인하면
`location.href = null` 이 되어 엉뚱한 주소로 갔습니다. `/store` 로 채웠습니다.

**`make-logo.py` 도 고쳤습니다.** 경로가 옛 자리(`~/Desktop/Rebind`)를
가리키고 있어서 **지금은 돌지 않는 상태**였습니다 — 프로젝트가
`05_개발프로젝트/` 아래로 옮겨졌는데 스크립트가 따라오지 않았습니다.
경로를 맞추고 Re:O-S 를 대상에 더했습니다.
아이콘 PNG 셋은 마크가 같아서 Re:Store 것을 그대로 복사했습니다.

### 만들다 잡은 것들

- **긴 금액이 카드를 화면 밖으로 밀어냈습니다.** 그리드 `1fr` 은 안에 든
  것보다 작아지지 않습니다(`min-width:auto`). `120,000,000원` 이 칸을 밀어
  오른쪽 카드가 잘렸습니다. `minmax(0,1fr)` 로 바꿨습니다.
- **닫힌 시트가 PC 에서 화면 한복판을 먹었습니다.** 모바일은 화면 밖으로
  내려가 괜찮은데, PC 에서는 가운데에 투명하게 남아 그 자리 클릭을 전부
  가로챘습니다. 미끄러져 내려간 **뒤**에 감추도록 했습니다 —
  바로 감추면 닫히는 모습이 안 보이고 그 자리에서 툭 사라집니다.
- **프로젝트 머리글의 사실 줄이 붙어 나왔습니다.** flex 칸인데 이어 붙여서
  "(D-2)담당" 처럼 됐습니다. 하나씩 `<span>` 으로 쌌습니다.
- **단계 막대가 현재 단계를 안 보여 줬습니다.** 생산 단계면 줄 오른쪽에 있어
  화면 밖입니다. 그릴 때마다 가운데로 굴려 둡니다.
- **원가표의 항목 이름이 세 줄로 쪼개졌습니다.** 표는 가로로 굴러가므로
  좁다고 칸을 줄일 이유가 없습니다. 첫 칸에 폭을 줬습니다.

### 문지기를 실제로 통과시켜 봤습니다

시연 자료로 링크를 켜고 운영 함수를 직접 불렀습니다.

- 고객 링크 → 업체 이름·원가·단가·내부 메모가 **하나도 안 나왔습니다.**
- 업체 링크 → 핀 없이 401, 틀린 핀에 "남은 횟수 9회", 맞는 핀에 발주서.
- 업체가 `qty:1` 과 `due_on:2030-01-01` 을 함께 보냈지만 **무시**됐습니다.
  날짜와 메모만 저장되고 활동 기록에 "대성제본 (업체 화면)" 으로 남았습니다.
- 확인이 끝난 뒤 열쇠와 핀을 새로 뽑고 링크를 껐습니다 —
  시험하며 쓴 값이 기록에 남았기 때문입니다.

**한 가지 못 막는 것을 찾았습니다.** 사양의 **제품명**은 업체가 만들려면
있어야 해서 그대로 나갑니다. 거기에 고객사 이름을 적어 두면
("2026 한빛 양장 다이어리") 업체가 최종 발주처를 알게 됩니다.
코드로는 지울 수 없어서, **업체 링크를 만들 때 알려 주도록** 했습니다.

### 시험

브라우저에서 로그인 화면(모바일·PC), 오늘, 프로젝트 목록·보드,
프로젝트 상세 여덟 탭, 거래처, 일정, 직원 권한(금액 가림),
고객사 화면, 외주업체 화면을 그려 봤습니다. 콘솔 오류 없습니다.

⚠ **실제 계정으로 로그인해 본 것은 아닙니다.** 비밀번호가 없어서
자료를 손으로 넣어 화면만 확인했습니다. `ACTIVA / admin` 으로 한 번
들어가 보고, 프로젝트를 하나 만들어 저장까지 되는지 봐야 합니다.

---

## 2026-09-07 — 운영에 올렸습니다

저장소 `MotoDNA/Reos` 를 공개로 만들고 Pages 를 켰습니다.
형제 셋도 함께 올렸습니다(토글이 넷이 됐으므로 같이 올라가야 합니다).

`network-dna` push 가 `fatal: the remote end hung up unexpectedly` 를
뱉었는데, **실제로는 올라가 있었습니다.** `git ls-remote` 로 보니 원격이
이미 그 커밋이었습니다. 오류 문구만 믿고 다시 밀지 마세요 —
원격을 직접 확인하는 편이 빠릅니다.

`npx vercel --prod` 는 쓰지 않았습니다. `web/` 에 커밋 안 된 변경이 34건
있어서 로컬 배포하면 그것까지 함께 올라갑니다. `git push` 로 올려
커밋된 것만 반영했습니다.

올린 뒤 확인한 것 — `dnalabs.kr/os` 가 내려 주는 파일이 로컬과 해시까지
같고, `/bind`·`/call`·`/store` 에도 네 번째 토글이 들어갔으며,
콘솔에 오류가 없습니다.
