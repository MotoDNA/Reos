# Re:O-S 설치

이미 다 되어 있습니다. 이 글은 **처음부터 다시 깔아야 할 때** 보는 것입니다.
지금 상태는 [REOS.md](REOS.md) 에 있습니다.

---

## 1. 표 만들기

Supabase CLI 가 `Rebind` 폴더에서 link 되어 있습니다.

```bash
cd ~/Desktop/05_개발프로젝트/Rebind
supabase db query --linked -f ../Reos/sql/0001_reos.sql      # 표 스물 · RLS · 권한 · 보관함
supabase db query --linked -f ../Reos/sql/0002_company.sql   # 회사에 Re:O-S 열어 주기
supabase db query --linked -f ../Reos/sql/0003_portal.sql    # 핀 시도 표 · 문지기 권한
supabase db query --linked -f ../Reos/sql/0004_pin_rpc.sql   # 핀 세는 함수
```

번호 순서대로. `0001` 은 여러 번 돌려도 안전합니다(`if not exists`).

**먼저 있어야 하는 것** — Re:Call 의 `0001_init.sql`(도우미 함수)과
Re:Bind 의 `sql/0019_apps.sql`(`company_for_app`). 둘 다 이미 들어가 있습니다.

## 2. 서버 함수

```bash
cd ~/Desktop/05_개발프로젝트/Reos
supabase functions deploy reos-gate --no-verify-jwt --project-ref izrtclsqhsgkuwsffifn
```

`--no-verify-jwt` 를 빼면 **고객사와 외주업체가 링크를 못 엽니다.**
계정이 없는 사람이 부르는 함수라 로그인이 있을 수 없습니다.

확인:

```bash
curl -s -o /dev/null -D - -X OPTIONS \
  https://izrtclsqhsgkuwsffifn.supabase.co/functions/v1/reos-gate \
  -H "Origin: https://dnalabs.kr" -H "Access-Control-Request-Method: POST" \
  | grep -i access-control-allow-origin
# → https://dnalabs.kr 가 그대로 돌아오면 통과
```

## 3. 앱 올리기

```bash
cd ~/Desktop/05_개발프로젝트/Reos
git push          # GitHub Pages 1~2분
```

## 4. dnalabs.kr/os 로 이어 주기

`network-dna/web/vercel.json` 의 `rewrites` 에 한 줄:

```json
{ "source": "/os", "destination": "https://motodna.github.io/Reos/reos.html" }
```

```bash
cd ~/Desktop/05_개발프로젝트/network-dna
npx vercel --prod --scope chhanj40-5991s-projects
```

`--scope` 를 빼면 `Not authorized` 가 납니다.

## 5. 회사 열어 주기

```sql
update public.companies
   set apps = array_append(apps, 'reos')
 where code = '회사코드' and not ('reos' = any(apps));
```

⚠ **덮어쓰지 마세요.** `apps = '{reos}'` 로 적으면 그 회사가 쓰던
Re:Bind·Re:Call 이 그 자리에서 막힙니다.

회사와 계정을 새로 만드는 것은 Re:Call 의 `setup-admin.sh` 가 합니다.
**계정 먼저, 회사 SQL 은 나중** — 이미 있는 회사 코드를 거부합니다.

## 6. 로고

```bash
python3 ~/Desktop/05_개발프로젝트/Rebind/make-logo.py
```

네 앱과 홈페이지 파비콘까지 한 번에 넣습니다. **저장소 넷을 각각 커밋**해야 합니다.
